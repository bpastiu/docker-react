language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t pastiubogdan/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true pastiubogdan/docker-react npm run test